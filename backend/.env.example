# 営業日報システム バックエンド環境変数設定
# このファイルを .env にコピーして使用してください

# ===================
# データベース設定
# ===================

# 接続URL（この設定が優先される）
# DATABASE_URL=mysql://user:password@localhost:3306/sales_report_system

# 個別設定（DATABASE_URLが未設定の場合に使用）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=sales_report_system

# コネクションプール設定
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# ===================
# アプリケーション設定
# ===================
APP_NAME=営業日報システム API
DEBUG=false

# ===================
# JWT認証設定
# ===================
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_SECONDS=86400

# ===================
# CORS設定
# ===================
# カンマ区切りで複数指定可能
CORS_ORIGINS=["http://localhost:3000"]
